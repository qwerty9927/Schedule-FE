function Import({ style }) {
  return (
    <div className={style.import}>
      <button><i className="fa-solid fa-file-import"></i><span>Import</span></button>
    </div>
  )
}

export default Import